// Firebase REST authentication is intentionally configured through Vite environment
// variables in src/services/firebaseAuth.js. This compatibility file contains no secrets.
export const firebaseProjectId = import.meta.env.VITE_FIREBASE_PROJECT_ID
